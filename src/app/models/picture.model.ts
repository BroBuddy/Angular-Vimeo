export class Picture {
    public height: number;
    public width: number;
    public link: string;

    constructor(height: number, width: number, link: string) {
        this.height = height;
        this.width = width;
        this.link = link;
    }
}
